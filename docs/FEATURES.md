# 功能清单

> 状态：`[x]` 已完成 / `[~]` 做了但待打磨 / `[ ]` 未开始
> 每完成一块就更新这里，并在 git 里留下可回滚的提交。

## 共享层 `packages/shared`

- [x] 领域类型（身份、时段、提交记录、后台配置）
- [x] 默认配置 + 配置归一化（缺字段自动补默认值，便于版本升级）
- [x] 时段锁定算法（`covered` / `overlap` 两种模式）
- [x] 季节判定（中午太热 / 太冷）
- [x] 日期网格生成（未来 N 天 × 三时段）

## 后端 `apps/server`

- [x] SQLite 建表与迁移
- [x] `config.json` 运行时读写（后台改完立即生效）
- [x] 管理口令鉴权（HMAC 签名 token）
- [x] 公开接口：读配置 / 提交 / 查自己状态
- [x] 管理接口：登录、配置读写、列表、详情、改状态、备注、删除、导出
- [x] 生产环境托管前端静态产物 + SPA 回退

## 用户端 `apps/web`

- [x] **填写页与状态页拆成两条路由**（`/date/:role` / `/status/:role`），各自只干一件事，靠重定向互相分流

- [x] 入口页：红头文件风标题 + 四张身份卡片
- [x] 身份关闭：卡片仍在，点进去弹「关停文案」+ 艺术字
- [x] **向导式填写：一个问题一页**，答完自动翻页，带步骤进度点
- [x] 第 1 页：昵称 —— **好宝宝 / DAD&MUM 跳过这一页**
      （牛马心里有数，还让人自报家门很怪），名字走后台可配的默认值「宝宝」「爸妈」，
      所以这两个身份是 **5 步**而不是 6 步
- [x] 第 2 页：**月历**（**年 / 月两个下拉栏** + 左右切月）
- [x] 月历标出**休息日**（暖色底）和**约不上的日子**（划掉 + 🔒），底部有图例
- [x] 第 3 页：时段只在所选日期上选，**点一下选中、再点一下取消**
- [x] 第 3 页：早上只展示不可选，点了弹起床气文案
- [x] 第 3 页：被工作冻住的时段，冰面卡片 + 悬浮提示（点别处 / 3 秒后 / 再点同一格都会关掉）
- [x] 第 3 页：**中午 🥵 / 晚上 🥶** 冷热小人（分时段，不是都堆在中午），月份可在后台配
- [x] 支线：自己定一个时间——**日期沿用上一步**，只填「几点」「干嘛」两栏大字输入
- [x] 第 4 页：地点 + 【你定吧】躲闪按钮
- [x] 第 5 页：**见面要求 + 留言**合成一页（都是自由文本，7 步 → 6 步）
- [x] 全锁的一天给明确引导，并把「自己定一个时间」提成主按钮
- [x] 步进条下面提示「填一半关掉也不怕」，并显示「已选 N 个时间」（**每次慢挪一小步就停**，3 秒内的点击只让它再挪一步，之后自己回原位，全程不出视口）
- [x] 第 5 页：留言
- [x] 第 6 页：确认（可删掉某个时间 / 再加一个时间）→ 提交
- [x] 提交成功页：「牛马正在骑马赶来的路上……」，只给「有问题微信联系牛马」的提示
- [x] 回执**永远**显示牛马回复；好友自己提的见面要求也一并显示
- [x] 提示文案分三行，「请直接微信联系牛马」单独一行且禁止折行
- [x] 同设备再次进入显示已提交状态，直到**被驳回**或**约会日过了第 N 天**才放回填写
- [x] **被驳回单独一屏**：显示原时间 + 牛马回复（一定展示）+「重新填一份」→ **回入口页从选身份开始**
- [x] 重新填写时**复用上次的昵称 / 地点 / 留言**（不带时间，避免误选掉不能用掉的时段）
- [x] 回执上说明「这份只存在本机浏览器里」，把换设备的情况引到微信
- [x] 待审批 / 已通过 / 已驳回三套文案 + 对应小人（🥵 / 🥳 / 😭 带落泪动画，纯 CSS）
- [x] **暂停营业总开关**：入口页与填写页整屏拦掉，已交过的人仍能看回执，后端也拒绝新提交
- [x] 暂停营业页做成**小店拉闸关门**：卷帘门下落带回落、落定后高光扫过、营业灯绿转红、
      「暂停营业」牌子随后挂出并摇摆（纯 CSS，无外部素材）
- [x] 暂停营业时**所有开单入口都堵死**：入口页 / 填写页整屏拦、驳回页不给「重新填一份」、
      提交与直连接口都 403；但已交过的人仍能看自己的回执与驳回原因
- [x] 总开关提到**后台最上面**做成状态横条（绿=接单中带呼吸点 / 红=暂停营业 + 一键切换），
      走单独的 `PUT /admin/site`，不会覆盖配置页未保存的改动
- [x] **所有时间相关的输入都「可敲 + 可选」**（全站排查过）：
      时段、工作制、冷热月份（12 个格子 + 输入框）、可选天数、重填天数、好友端「几点」
- [x] 日历年 / 月**刻意保持 `<select>`**（微信内核的 datalist 支持不稳，好友端要稳）
- [x] 草稿本地暂存（误关 / 刷新不丢）

## 后台 `apps/web/src/pages/admin`

- [x] 口令登录
- [x] 审批列表 + 筛选，**每条突出人物 / 时间 / 地点 / 留言**
- [x] **详情紧贴被点的单子底下展开**，不再跑到列表最下面
- [x] 设备号从主界面移除，只保留在原始 JSON 里
- [x] 改状态 / 审批意见 / 代填地点 / 删除
- [x] 好友提的**见面要求**只读展示（列表一行 + 详情一行）
- [x] **待审批数量挂在标签页角标上**，浏览器标题也变成「(3) 牛马后台」——
      瞥一眼标签页就知道有几单等着，不用来回刷
- [x] **列表行内直接「批准 / 驳回」**，不用展开详情、也不用先勾选走批量栏
- [x] 每行显示**「9月13日 23:49 交的」**，配合排序才看得出新旧
- [x] **导出跟随筛选**：搜出 18 条就导 18 条，并标出「只导当前筛出来的」
- [x] **一键复制回复话术**：好友那边没有任何通知，牛马审完点一下复制，直接粘到微信发过去
- [x] **牛马年报**：一共几位好友、收了多少单、通过率、被约最多的时段与星期、
      最晚一次几点交的、最常去的地方……
- [x] **默认排序：审批中优先、今天交上来的优先**，其次按提交时间倒序；
      今天交上来的会打个「今天」黄色标，排序不是黑盒
- [x] **关键字搜索：昵称 / 身份 / 状态 / 时间 / 地点 / 见面要求 / 留言 / 审批意见**
      全都进搜索范围（客户端即时筛，全选只作用于筛出来的）
- [x] 审批意见的**快捷短语**（一点即填）
- [x] **审批意见支持换行**：输入框能跟着内容长高，回执/驳回页用 `pre-wrap` 原样显示
- [x] **按约会日期排序**（最近要赴的排最前，过去的沉底）+ **日期窗口筛选**
      （今天 / 未来 7 天 / 未来 30 天；一条里任意一天命中就算）
- [x] **导出 CSV**（带 BOM，Excel 打开不乱码）
- [x] **批量操作**：勾选多条 → 批量批准 / 批量驳回 / 批量删除
- [x] 列表上方**全选框**（含半选态，只作用于当前筛选结果）
- [x] 导出全部
- [x] 系统配置编辑（含「约会日后第几天可重填」、**接单总开关**、**每格时段是否可选**）
- [x] **保存条钉在视口底部**：有改动立刻变红并出现按钮，不用滑到页面最下面；
      支持「放弃改动」；带未保存改动切页 / 关页会拦一下
- [x] 冷热月份：12 个可点格子 + 可敲输入框（多值字段不能用 datalist），
      分别对应「中午太热」与「晚上太冷」；**全不选就是全不选，不会被改回默认**
- [x] **配置备份**：导出 / 导入 / 一键恢复默认（导入后先标脏，确认再存）
- [x] **PWA manifest**：可以「加到主屏幕」，用起来像个小程序
- [x] **上线预检脚本 `pnpm preflight`**：查 Node 版本 / 构建产物 / 静态资源 /
      数据目录可写 / 环境变量 / 服务是否活着，输出 go / no-go
- [x] **牛马吉祥物 `NiumaMark`**：手绘 SVG（牛角 + 马耳 + 马鬃 + 牛斑），
      用在入口页徽标、后台抬头、卷帘门招牌、冻结时段徽章
- [x] **四套皮的字体**：宋体 / 楷体 / **站酷庆科黄油体** / 仿宋（GB/T 9704）；
      前三套走系统字体栈零字节，宝宝那套是 134 KB 子集 Web 字体
- [x] **全站动画**：切一步换一种进场方式（硬上浮 / 柔上浮 / 弹入 / 匀速划入）；
      日历格子每格 +11ms 落成一道波；时段卡片依次弹出；进度小点逐段点亮；
      身份卡依次飞入；回执滑入 + 公章「砸」下来。
      注意 `animation-fill-mode` 用 `backwards` 不用 `both`
      —— `both` 会把 `transform` 钉死，之后 hover 和按下的缩放全失效
- [x] **四套皮的动效**：每套一个**只有它有**的招牌动作 ——
      兄弟「红头双线啪地印出来」、姐妹「柔光扫过红头」、宝宝「纸张粉光呼吸 + 爱心心跳」、
      DAD&MUM「匀速划出来」（不弹不花）
- [x] **四套皮的文字对比度全部达标**（大字 ≥3、正文 ≥4.5）；宝宝和姐妹原来偏浅，已按量出来的数值调深
- [x] **宝宝主题换字体解决发虚**：幼圆比黑体轻约 22%，小字发虚。
      走过「列选择器名单」和「合成加粗」两版弯路，最后换成站酷庆科黄油体
- [x] **四个身份四套皮**：背景 / 字体 / 边框方圆 / 配色 / 装饰全不一样。
      好兄弟直、好姐妹精致、好宝宝甜软、DAD&MUM 正式公文。
      入口页四张卡各挂各的主题；后台可逐身份切换主题
- [x] **动画 emoji**（Google Noto Animated Emoji，CC-BY 4.0，署名见 `docs/CREDITS.md`）：
      入口页四张身份卡、冷热小人、驳回页…
      原素材 512px / 1–2 MB，压成 **112px 抽帧 WebP，单个 40–95 KB**；
      `<BigEmoji>` 自带 `onError` 兜底，放不出来就退回普通 emoji 字符
- [x] **文案走「牛马自嘲」路线**：副标题「上班时间概不受理」、
      好兄弟「喝酒随叫随到，早起恕不奉陪」、好宝宝「想我了就直说，别让我猜」…
- [x] **文号自动生成**（`牛马字〔YYYY〕1 号`，跨年自动更新），后台不可改
- [x] **日历的视觉语言**：底色只说「能不能约」（能约=淡绿、约不上=压暗划掉），
      休息日/今天/已选过一律用小标记，不跟底色抢
- [x] **「让你定」躲 5 秒后自己滑回原位**；活动范围以原位为中心（±150×±110）并夹在视口内，
      不会飘到离输入框很远的地方；四套皮都会动（公文皮匀速不弹）；
      开了「减少动态效果」也照样躲（步子减半），不会变成「只换个字」
- [x] **排版原则**：短标签 nowrap 永不断行；列表行一行到底 + 省略号；
      「日期+时段」条目内部不断；长正文用 `text-wrap: pretty/balance` 断得均匀

## 文档

- [x] `docs/REQUIREMENTS.md` —— 需求决策记录（已拍板 / 待定分开写）
- [x] `docs/FEATURES.md` —— 本文件
- [x] `docs/DEPLOY.md` —— 云服务器部署（systemd + Caddy 自动 HTTPS + 备份）
- [x] `README.md` —— 开发、调试、测试说明

## 工程

- [x] pnpm workspace + 全量 strict TypeScript
- [x] typecheck / build 脚本
- [x] 结构化日志：启动横幅、**配置逐字段 diff + 来源 IP**、提交与拒绝原因、后台鉴权、审批变更、删除
- [x] **日志轮转**：单个文件超过 `LOG_FILE_MAX_MB`（默认 5）就滚成 `.1`，只留一代
- [x] **限流**：提交 1 小时 120 次 / 登录 10 分钟 20 次（可用环境变量调），超了返回 429
- [x] `/api/health` 不再对外暴露提交条数
- [x] `LOG_FILE` 支持日志同时落文件（同步写，进程崩了也不丢最后几行）
- [x] 前端控制台日志：请求失败打出方法 / 路径 / 状态码 / 错误码
- [x] `scripts/smoke.mjs` 接口冒烟测试（41 项断言，退出码可用于 CI）
- [x] UI 冒烟**开跑先清一次浏览器存储**：草稿是按身份存在 localStorage 的，
      上一轮中途挂了会留下脏草稿，这一轮再点同一个时段就变成「取消选中」，
      后面的等待全超时 —— 报出来像功能坏了，其实是脏数据
- [x] 两套冒烟都会**先自愈站点总开关**再跑，并在**收尾时断言站点已恢复接单** ——
      一旦哪次跑完把它留在关闭状态，直接算失败，而不是静默毒害下一轮
- [x] 冒烟**不再整份 PUT 配置**：总开关走只改一个字段的 `/admin/site`；
      开跑前断言四个身份都是开的；收尾**整份比对配置与开跑前完全一致**
- [x] `scripts/ui-smoke.mjs` 用 CDP 驱动无头 Chrome 走一遍用户端向导并逐步截图（12 项断言，含日历切月、时段可取消、躲闪不出视口）
- [ ] 自动化测试（Vitest + Fastify inject）
- [ ] ESLint + Prettier
- [ ] CI
