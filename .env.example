# 复制为 apps/server/.env 后按需修改（后端启动时会自动读取）
HOST=127.0.0.1
PORT=8787
WEB_ORIGIN=http://localhost:5173
LOG_LEVEL=info
# 设了就额外把日志追加到这个文件（同时仍然打印到标准输出）
# LOG_FILE=/var/log/niumadate/app.log

# ---------------------------------------------------------------------------
# 后台口令与会话密钥
#
# **这两项不用填。** 不填的话，服务第一次启动会各随机生成一个：
#   - 存进 data/admin-password 和 data/session-secret
#   - 并在日志里打印一次，抄下来就能登后台
#
# 为什么不给默认值：这个仓库是公开的，任何写死的默认口令都等于公开。
# 会话密钥更严重 —— 知道它的人可以自己签一个合法 token，连口令都不用猜。
#
# 想自己指定，就取消下面两行的注释：
# ADMIN_PASSWORD=
# SESSION_SECRET=
#
# 详细说明（怎么改、忘了怎么办）见 docs/DEPLOY.md 的「管理员鉴权」。
# ---------------------------------------------------------------------------

# 可选：覆盖默认路径
# DATA_DIR=/opt/niumadate/apps/server/data
# WEB_DIST=/opt/niumadate/apps/web/dist
