# 复制为 apps/server/.env 后按需修改（后端启动时会自动读取）
HOST=127.0.0.1
PORT=8787
WEB_ORIGIN=http://localhost:5173
LOG_LEVEL=info
# 设了就额外把日志追加到这个文件（同时仍然打印到标准输出）
# LOG_FILE=/var/log/niumadate/app.log

# 部署到公网前必须改掉这两项，否则后台等于没锁
ADMIN_PASSWORD=niuma-dev
SESSION_SECRET=change-me

# 可选：覆盖默认路径
# DATA_DIR=/opt/niumadate/apps/server/data
# WEB_DIST=/opt/niumadate/apps/web/dist
